# Memory index

> **No lista todas las memorias.** Lo que no ves acá se recupera igual:
> `python3 Tools/kb/kb.py find "<términos>"`.
> Acá va sólo lo que tiene que **dispararse solo**: método, reglas del usuario y errores que no
> hay que repetir (`type: feedback`). Lo de consulta (`reference`, `project`) se busca.
> El hook `memoria-indice.sh` mantiene este archivo debajo de las 200 líneas.

## Cómo trabajar (reglas del usuario)

## Método: cuándo no creerle a lo que ves

## Errores que ya pasaron
